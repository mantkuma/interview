function abc(){
   [
    [1,2],
    [3,4],
    [5,6]
   ].forEach(ele=>{
    ele.forEach(el=>{
        if(el === 4) 
    })
    if(ele[0] === 1){
        return "firstLoop"
    }
   })
   return "kumar"
}

console.log(abc());